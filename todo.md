
TODO 2025-06 HI PRIO
=========

`fss_save.py`
в режиме "update".
скрипт тупо загружает файл `.index_hash.yaml`
и считает что все дириктории в этом индексе обязательно существуют и пытается в каждую директорию зайти.
при этом он не делает (я не уверен -- надо проверить) считывание реально существующих директорий.
тоесть если директорию удалили - то скрипт развалится.
а если директорию добавили - то скрипт ее не заметит.
Но это моя теория - как оно в скрипте на самом деле я не помню, нужно проверять.







TODO
=========


использовать упорядочненый словарь для YAML.
чтобы поля находились рядом и имели логику в расположении.
и чтобы у директорий сначала шла инфа о директории а потом содержимое.


- [x] Изменить формат времени - с timestamp числа на человеко читаемый формат
- [x] Добавить поддержку усечения точности времени
Добавить поддержку смещения времени по часовым поясам


Добавить запись незавершенного YAML - у него будет отдельное имя, и пометка что он незавершен.
В этом случае при обновлении будет подгружаться он и процесс продолжается с момента остановки.
Разумеется в этом случае нужно делать особый режим - подгрузка незавершенного файла.
Плюс нужно смотреть дату и время - если разница слишком большая (например одна неделя, то нужно перезапускать процесс).



- [x] Добавить повтор чтения файла с ошибкой PermissionError (с небольшой паузой).
      Это полезно для работы с сетевыми дисками (а еще с EncFS для windows - там есть такой баг).

Добавить игнорирование ошибок чтения файла - с пометкой в каталоге что была ошибка.

Добавить игнорирование ошибок записи каталога - с пометкой в директории выше что была ошибка.


## fss_save.py

TODO: add more flexibility - def filter_dir(path: Path) -> bool:

`g_meta_info_name = '?> sfs info <?'`

TODO: search silent changes. add recheck MD5 for unchanged files and warnings if file corrupted

TODO: add flag: "-i" (ignore all)
TODO: add flag: "--ignore-linux-hide"
TODO: add flag: "--ignore-wnd-hide"

TODO: add simple update function: various modes: update all date, update MD5



## Done


мне не нравится структура типа дерева.
у нее много проблем:
- отступы сжирают место. на глубоких уровнях пробелы могут занимать больше места чем информация.
- найдя запись в таком файле очень сложно понять путь по которому файл расположен, и невозможно этот путь скопировать.
- для записи файла нужно сформировать все дерево в памяти.
- для загрузки нужно загрузить весь файл, нельзя обрабатывать его по кусочкам.
- дерево дает красоту в текстовом редакторе - слево получается настоящее дерево.
  но проблема в том что такими продвинутыми редакторами никто не пользуется.
  и по факту эта красота не нужна даже мне.
  если я хочу посмотреть дерево - то можно сконвертировать в формат lst(DiskDir) и смотреть в файловой браузере что гораздо удобнее и функциональнее.
  плюс всегда можно написать конвертер в дерево, это легко.
  ну и я обнаружил что реальные файлы в реальных кэйсах просто не отображают дерево, потомучто файл весит 10 мегабайт, и парсер текстового редактора не делает разбор такого большого обьема строк.
- не возможно сделать поиск по пути.
- очень сложно редактировать файл - если нужно добавить содержимое директории вручную то нужно возиться с отступами.
Поэтому нужно переделать формат в вид:
словарь путей (не рекурсивный), где каждая запись имеет имя в формате path где указан относительный путь.
Это даст преимущества:
- в текстовом редакторе по прежнему будет видно стркутуру, просто ее нельзя будет сворачивать.
- можно искать по пути
- можно сохранять фрагменты, и выкидывать их из памяти, чтобы оптимизировать расход памяти.
- можно парсить по кусочкам - вытаскивать по одной записи, чтото делать с ней, и выкидывать ее из памяти, и брать следующую запись.
- человеку легко соединять файлы и редактировать их. потомучто не нужно возиться с отступами. можно просто закинуть контент в конец файла и быть увереным что он корректно присоединился к общей информации.


