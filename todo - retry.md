С учётом обновлённого комментария к `WhileWithRetryIO` (один путь, любое чтение/открытие, YAML-ветки безвредны для не-YAML) картина такая.

---

## Список мест, где **имеет смысл** рефакторинг на `WhileWithRetryIO` вместо своих циклов

1. DONE. **`fss_save.py` — функция `read_file_and_calculate_md5_retry`**  
   Сейчас: ручной `while attempt <= retries` + `time.sleep` + свой вывод ошибок.  
   Имеет смысл: это ровно повторяющиеся попытки **открыть и прочитать файл по пути** при `OSError`; политика совпадает с `WhileWithRetryIO` (кроме мелочей вроде текста логов и семантики «последняя попытка → `('', False)` без проброса» — это уже про сохранение поведения, не про пригодность класса).

---

## Список мест, где **цикла ретраев пока нет**, но **имеет смысл добавить** обёртку `WhileWithRetryIO` (когда будете делать TODO)

2. **`fss_utils.py` — `load_yaml_fss_file_stream_callback`** (рядом TODO про `retries` / `retries_pause`)  
   Имеет смысл: обернуть **как минимум** `open` + начало стрима в `while r: with r.attempt():`, чтобы повторять попытку при транзиентном I/O до парсинга. Повтор целого файла после сбоя посередине стрима — отдельное решение (часто просто «с начала»).

3. **`fss_utils.py` — `load_yaml_fss_file_stream_iter`** (тот же TODO)  
   То же самое: единая точка ретраев вокруг открытия и прохода лоадера, по тому же паттерну, что и `load_yaml`.

---

## Что **не** попадает в список «вместо циклов»

- **`load_yaml` в `fss_utils.py`** — уже на `WhileWithRetryIO`; рефакторинг не нужен.  
- **`fss_merge.py` / `fss_check.py` / вызовы `load_yaml` в `fss_save.py`** — ретраи уже внутри `load_yaml`; менять на прямой `WhileWithRetryIO` снаружи нет смысла (дублирование).  
- **Другие `.py` в репозитории** — по поиску других ручных ретрай-циклов с `time.sleep` кроме `read_file_and_calculate_md5_retry` не находится.

Итого по сути **один** явный рефакторинг «цикл → `WhileWithRetryIO`» сегодня — **`read_file_and_calculate_md5_retry`**; плюс **два** места, где ретраев ещё нет, но их логично ввести через тот же класс при реализации TODO — **оба потоковых загрузчика YAML в `fss_utils.py`**.
